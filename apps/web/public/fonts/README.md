Place Janna LT font files here for full typography:

- `JannaLT-Regular.woff2`
- `JannaLT-Bold.woff2`

If installed on Windows, the site uses the local font automatically.
Until then, **Tajawal** (Google Fonts) is used as Arabic fallback.
